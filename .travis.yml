language: php
sudo: false

php:
  - 5.6
  - 7.0
  - hhvm

install:
  # Install Composer.
  - composer install

script:
  # Run PHPUnit tests.
  - ./vendor/bin/phpunit -c ./phpunit.xml --coverage-text
